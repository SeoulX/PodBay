# Application Settings
APP_NAME=PodBay API
APP_VERSION=1.0.0
DEBUG=false

# Server Settings
HOST=0.0.0.0
PORT=8000

# Kubernetes Settings
KUBECONFIG_PATH=~/.kube/config
DEFAULT_CONTEXT=

# CORS Settings
ALLOWED_ORIGINS=["*"]
ALLOWED_METHODS=["*"]
ALLOWED_HEADERS=["*"]
