ELASTICSEARCH_HOST=http://es-v4.media-meter.in:80
# Leave empty to monitor all services, or specify a service name
SERVICE_NAME=
# Leave empty to monitor all environments, or specify an environment name
ENVIRONMENT=
# Set LOOKBACK_MINUTES to match MONITOR_INTERVAL (in minutes) to prevent duplicate alerts
# MONITOR_INTERVAL=60 seconds = 1 minute, so LOOKBACK_MINUTES=1
LOOKBACK_MINUTES=180
MONITOR_INTERVAL=60
SINGLE_RUN=true

